spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/goview?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Mayuan112233
    druid:
      initial-size: 10
      max-active: 100
      min-idle: 30
      max-wait: 40
      validation-query: SELECT 1
